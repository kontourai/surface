[
  {
    "scope": "author:briananderson1222 branch-prefix:chore/block-hygiene",
    "reason": "Block hygiene from a portfolio audit: creates this repo's first delivery/DECLARED (a repo without one fails bundle-required on its first agent-delivered change, with a message that reads like the change is at fault), adds .github/dependabot.yml watching npm and github-actions, and moves the pinned trust-verify action to the build that names which DECLARED condition failed (flow-agents#1015 — this repo was one of the consumers found still pinned behind it, flow-agents#1018). Config only; no source change, nothing for a bundle to reconcile. Verification in lieu of a bundle: this PR's own CI, including the freshly bumped Trust Verify. Scope is paired with author: per the ADR 0022 2026-07-03 addendum, since branch-prefix alone resolves against a pusher-controlled ref.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-07-27 session: scoped-DECLARED-exemption route)",
    "declared_at": "2026-07-27T18:00:00Z"
  },
  {
    "scope": "author:dependabot[bot] branch-prefix:dependabot/",
    "reason": "Dependabot dependency-bump PR; no agent delivery involved, nothing to reconcile. Compound scope binds the platform-set actor identity to the bot's branch namespace per ADR 0022's fork-PR guidance, paired per the 2026-07-03 addendum since branch-prefix alone resolves against a pusher-controlled ref. Added because the dependency watch landed portfolio-wide before any repo except one had this exemption, so the watch's first harvest failed Trust Verify everywhere at once — the gate was right to refuse, and this is the documented route rather than a weakening. Known and accepted cost: if the bot identity rotates this stops matching and dependency PRs break loudly, which the current pinned action reports by naming the failed condition (flow-agents#1015).",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-07-27 session: scoped-DECLARED-exemption route)",
    "declared_at": "2026-07-27T20:00:00Z"
  },
  {
    "scope": "author:briananderson1222 branch-prefix:chore/dependabot-exemption",
    "reason": "The governance-file change adding the dependabot exemption above. Touches delivery/DECLARED only; no source or behaviour changes and no command-backed agent-delivery claims to reconcile. Scope is paired with author: per the ADR 0022 2026-07-03 addendum.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-07-27 session: scoped-DECLARED-exemption route)",
    "declared_at": "2026-07-27T20:30:00Z"
  },
  {
    "scope": "author:kontourai-releases[bot] branch-prefix:release-please--",
    "reason": "release-please automation PR; no agent delivery involved, nothing to reconcile. Actor verified against live release PRs (opened by the kontourai-releases GitHub App) rather than assumed. Paired with author: per the ADR 0022 2026-07-03 addendum — branch-prefix alone resolves against a pusher-controlled ref. Known and accepted cost: a rotated bot identity stops matching and the release train breaks LOUDLY — the pinned action names the failed condition (flow-agents#1015); thirteen silent days was the bug, a loud break is the correct outcome (flow-agents#1011). Provisioned portfolio-wide after a live release PR failed on exactly this gap — the fourth worked example for flow-agents#1017.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-07-27 session: scoped-DECLARED-exemption route)",
    "declared_at": "2026-07-27T22:00:00Z"
  },
  {
    "scope": "author:briananderson1222 branch-prefix:chore/release-exemption",
    "reason": "The governance-file change adding the release-automation entry above. Touches delivery/DECLARED only. Scope paired with author: per the ADR 0022 2026-07-03 addendum.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-07-27 session: scoped-DECLARED-exemption route)",
    "declared_at": "2026-07-27T22:00:00Z"
  },
  {
    "scope": "author:briananderson1222 branch-prefix:chore/ts7",
    "reason": "TypeScript 7 with the coupled types:[node] tsconfig change (tsgo no longer auto-includes @types/node; TS2591 on node: builtins), the change a bot bump cannot carry. Proven on the relay canary (relay#39); full verify green here. Supersedes the bot's red dev-dependencies group. Scope paired with author: per the ADR 0022 2026-07-03 addendum.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-07-28 session: scoped-DECLARED-exemption route)",
    "declared_at": "2026-07-28T04:00:00Z"
  },
  {
    "scope": "author:briananderson1222 branch-prefix:chore/flow-agents-local-kit",
    "reason": "Pins @kontourai/flow-agents 5.5.0 as an exact local devDependency and adds the workflow:sidecar / workflow:validate-artifacts scripts, matching the one proven configuration (the marketing-site repo). With bundle-required now enforced portfolio-wide, this repo demanded delivery bundles while providing no declared means to author them: bundle authoring worked only on machines with a coincidental global kit install, and sandboxed delegates without network access could not produce bundles at all. Dev-tooling-only change: no source, no behaviour, no shipped-dependency change, nothing for a bundle to reconcile. Verification in lieu of a bundle: this PR's own CI runs the full verify matrix, and the kit's bins resolve from the local install. Scope is paired with author: per the ADR 0022 2026-07-03 addendum, since branch-prefix alone resolves against a pusher-controlled ref.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-07-28 session: owner accepted rollout recommendation)",
    "declared_at": "2026-07-28T21:00:00Z"
  },
  {
    "scope": "author:briananderson1222 branch-prefix:fix/omission-fails-open",
    "reason": "Attestation gaps now require a value rather than a key: metadata {identityProof: \"\"} no longer promotes an attestation from weak to valid, empty objects/arrays count as blank, and reads are own-property only via Object.hasOwn (matching waiver.ts's prototype-forgery guard). Touches src/trace-analysis.ts and one new test only; does NOT touch the versioned status function f, so it carries no hachure spec-conformance risk. Agent-delivered outside builder.build, so no trust bundle exists to reconcile. Verification in lieu of a bundle: 564/564 tests pass, typecheck clean, and three fault injections (string presence-of-value reverted, empty object/array guard removed, Object.hasOwn downgraded to bracket access) each turned the new tests red with a byte-identical restore. Independent report-only review returned CHANGES_REQUESTED and all findings were addressed or explicitly re-scoped: the statusFunctionVersion finding led to withdrawing the status.ts half of this change entirely (surface#200), and the !policy boundary is recorded there as unsettled rather than closed. Owner-authorised transitional exemption (2026-07-29 session); subsequent work on this arc goes through the delivery flow rather than a declaration.",
    "approved_by": "briananderson1222 (2026-07-29 session: declare around the two open PRs, then prove it going forward)",
    "declared_at": "2026-07-29T17:05:00Z"
  },
  {
    "scope": "author:briananderson1222 branch-prefix:feat/provenance-display-names",
    "reason": "PR #225 / issue #224: canonical reader-facing display names for the evidenceType/method/status enums, one table consumed by both shipped renderers. Agent-delivered outside a bundled workflow, so no trust bundle exists to reconcile. Verification in lieu of a bundle: full `npm run verify` green at both commits, run independently by the implementer AND a fresh-context reviewer (sha pinned as the first log line); schemas/ proven untouched (parity suite re-run explicitly; empty diff) - the hachure byte-identity constraint this repo enforces; three author fault injections (minting 'Pending', deleting a table entry, reintroducing 'Never run') each caught with byte-identical restores. Independent review returned CHANGES_REQUESTED with one reproduced MEDIUM introduced by the diff - `attestation` is a member of BOTH enums and requirement rows guessed the axis by lookup order, rendering a required method as the evidence-type's label - fixed by carrying the axis per group and pinned by a test that also asserts the two tables disagree on the shared key; the review's second MEDIUM (pre-existing raw status enums in the divergence banner) fixed in the same round; its two copy findings adopted. DISCLOSED GAPS, filed as #226 rather than absorbed: the synonym test is a denylist and cannot catch a NEW synonym (no console-DOM allowlist assertion exists); gloss text is not drift-locked between module and spec doc; transparency-gap types still render raw - the adjacent axis, same failure mode, next table. Scoped to this author and branch only.",
    "approved_by": "brian.anderson1222 (2026-08-17 session: 'do it.. drive all to batched merge' following 'each of those issues needs a fresh pair of eyes before pickup')",
    "declared_at": "2026-08-17T07:30:00Z"
  },
  {
    "scope": "author:briananderson1222 branch-prefix:chore/pnpm-migration",
    "reason": "Wave 2 of the org-wide npm -> pnpm 11 move (canary ui#69; plan tracked from station#516). Lockfile converted with pnpm import: 241 resolved versions, all identical to package-lock.json, zero drift. Dependency install scripts are blocked by default, with esbuild@0.28.1 the single version-pinned allowBuilds entry, added from evidence because the first strict install refused with ERR_PNPM_IGNORED_BUILDS naming it; minimumReleaseAge 1440 requires a newly published version to be 24h old before it can resolve. CI installs with pnpm install --frozen-lockfile after a SHA-pinned pnpm/action-setup in ci.yml, pages.yml, publish-npm.yml (three jobs) and trust-verify.yml, each passing package_json_file because every job checks out into surface/. Publishing stays on npm publish and npm run verify stays the verify command, so this action's contract string does not move. Two scripts read through node_modules and both were verified rather than assumed to survive pnpm's isolated layout: release-trust-bundle.mjs reads node_modules/hachure/package.json and node_modules/hachure/conformance, and hachure is a direct devDependency so pnpm symlinks it at the root (confirmed on disk); build-mcp-command.mjs derives a bundled package root with lastIndexOf(\"node_modules\"), which lands correctly inside node_modules/.pnpm/<pkg>@<ver>/node_modules/<pkg>. No shipped source change: the packed file list is identical to the published 3.2.0 tarball (283/283) and no pnpm-* file or lockfile enters it. Agent-delivered in an owner-attended session outside builder.build, so no trust bundle exists; Step 1 fresh verify passes. Verification in lieu of a bundle: npm run verify green end to end locally (content boundary, decisions, doc links, generated boundaries, typecheck, node --test suites, docs build, UI assets, console token drift, package contents 283 files, external adapter, package smoke, surface summary, and 56 Playwright tests) and in this PR's own CI; fault injections recorded in the PR body -- a manifest/lockfile mismatch fails with ERR_PNPM_OUTDATED_LOCKFILE and removing the allowBuilds entry fails with ERR_PNPM_IGNORED_BUILDS naming esbuild@0.28.1. Scope is paired with author: per the ADR 0022 2026-07-03 addendum, since branch-prefix alone resolves against a pusher-controlled ref.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-09-03 session: owner-directed pnpm migration across all repos, scoped-DECLARED-exemption route)",
    "declared_at": "2026-09-04T01:31:15Z"
  },
  {
    "scope": "author:briananderson1222 branch-prefix:ci/publish-path-dry-run",
    "reason": "Adds a publish-path dry-run job to ci.yml, rolled out from kontourai/ui#70. After the org-wide npm -> pnpm 11 move, every migrated repository had proven install, build, test and pack, and none had proven publish: npm publish runs only on a tag, against a pnpm-installed tree, under a setup-node that also sets registry-url -- a combination the verify jobs never exercise, and it is that .npmrc which npm publish reads. This job mirrors that environment on every pull request and stops one step short of the registry write. It cannot publish: --dry-run performs no write, the job requests no id-token permission so provenance cannot be minted, and no registry credential is provided. Workflow-only change: no source, no dependency, no lockfile and no shipped content is touched, so nothing for a bundle to reconcile. Agent-delivered in an owner-attended session outside builder.build, so no trust bundle exists; Step 1 fresh verify passes. Verification in lieu of a bundle: the job is proven green on a hosted runner in ui#70, where its output rather than its exit code is the evidence -- 'npm notice total files: 289' then 'Publish path proven from a pnpm-installed tree: 289 files assembled', with the registry declining only the already-published version. Fault injections recorded there: a type error making prepack fail is reported as 'assembled no tarball'; crafted logs prove the strict branch rejects a 403 while tolerating only the named version-exists refusal, and rejects a zero-file package that exits zero. ui#70's first CI run FAILED and that is disclosed rather than hidden: GitHub runs run: blocks as bash -e, which set -uo pipefail does not clear, so npm's expected non-zero exit killed the step and every assertion below was unreachable -- the gate had no body on CI while passing locally, because it had been tested with bash rather than bash -e. The version rolled out here carries the || STATUS=$? fix and the comment explaining why. The job's action pins follow THIS repository's existing convention rather than importing ui's. Scope is paired with author: per the ADR 0022 2026-07-03 addendum, since branch-prefix alone resolves against a pusher-controlled ref.",
    "approved_by": "brian.anderson1222 (delegated approval, 2026-09-04 session: owner-directed publish-path gate rollout, scoped-DECLARED-exemption route)",
    "declared_at": "2026-09-04T05:19:12Z"
  }
]
